<% provide(:title, 'Sign up') %>
<% provide(:button_text, 'Create my account') %>
<h1>Sign up</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%#= form_for(@user, url: signup_path) do |f| %>
    <%# |f|というオブジェクトはHTMLフォーム要素に対応するメソッドが呼び出されると、@userの属性を設定するために特別に設計されたHTMLを返す。 %>
    <%# 上記のRailsのformアクションは、「/signupというURLへのPOSTである」と自動的に判定してくれる。 %>
    
    <%= render 'form' %>
    <%# formパーシャルを代入　%>
    
  </div>
</div>