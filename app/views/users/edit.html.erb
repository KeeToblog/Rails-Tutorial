<% provide(:title, "Edit user") %>
<% provide(:button_text, 'Save changes') %>
<h1>Update your profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <%# form_forメソッドはPATCHリクエストをサーバーに送信する。Railsはユーザーが新規なのかDBに存在するのか判断してPOSTとPATCHを使い分ける %>
    <%# ActiveRecordのnew_record?論理値メソッドを使って区別する %>
    
    <%= render 'form' %>
    <%# formパーシャルを代入　%>
    
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <a href="http://gravatar.com/emails" target="_blank" rel="noopener">change</a>
      <%# target="_blank"でリンク先を新しいタブで開けるようになる。noopenerによりフィッシングサイトのようなコンテンツの導入を防げる %>
    </div>
  </div>
</div>